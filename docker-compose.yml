services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: showroom-vote:local
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - showroom_data:/app/data

volumes:
  showroom_data:
